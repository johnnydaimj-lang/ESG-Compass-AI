# ESG 快讯 — MVP 交付汇总

> 项目根目录：`D:\学习\ESG智能体\ESG Compass AI\esg-radar-mvp`
> 交付时间：2026-07-28
> 对应产品说明：`../产品说明.md`

---

## 项目概述

**一句话目标**：让网页浏览者 3 分钟扫完当下全球 ESG 变化趋势，并能够对重点领域相关的文件找到原文和解读。

**第一用户**：企业管理层。核心场景：打开 → 扫重点 → 清楚对企业运营的影响及应对建议。

**技术栈**：Next.js 16 (Turbopack) + React 19 + Tailwind CSS 4 + TypeScript

---

## 里程碑进展

### M1 — 数据管道 ✅

| 编号 | 任务 | 状态 |
|------|------|------|
| T1.1 | 信源配置 — `data/sources.json`（8 个初始信源） | ✅ |
| T1.2 | RSS 抓取模块 — `scripts/fetch-sources.mjs`，带 If-Modified-Since/ETag | ✅ |
| T1.3 | 无 RSS 信源抓取 — Playwright 抓列表页 | ✅ |
| T1.4 | 去重管理 — `data/seen_hashes.json`，按信源 ID + URL hash | ✅ |
| T1.5 | LLM 结构化 — `scripts/llm-structurer.mjs`，按 contentType 选 Prompt | ✅ |
| T1.6 | 免审发布 + 后台审校 — `lib/esg-data.ts` 读取 `data/contents.json`；`/review` 审校页面 + `/api/review` 接口 | ✅ |
| T1.7 | 定时 + 手动触发 — GitHub Actions 每周一/五 08:00 + `npm run pipeline` | ✅ |

**验收**：`npm run build` ✅ | `npm run pipeline` 可产生 contents.json ✅ | /review 页面可访问 ✅

### M2 — 首页 ✅

| 编号 | 任务 | 状态 |
|------|------|------|
| T2.1 | 顶栏布局 — 固定顶栏，Logo 虚线占位 + "ESG 快讯" | ✅ |
| T2.2 | ContentCard 组件 — 重要性/类型/地区/日期/标题/摘要/ESG 议题；主点击→详情页，右上角原文图标 | ✅ |
| T2.3 | 首页结构 — "今日重点" + "重点分析/解读"（Tab + 三栏，首卡头条标签），按时间排序 | ✅ |
| T2.4 | Tab 筛选 — 全部/政策/专家观点/学术/评级，切换过滤 contentType | ✅ |

**验收**：`npm run build` ✅ | 首页结构完整 ✅ | Tab 切换过滤正确 ✅ | 卡片链接（站内+原文）正常 ✅

### M3 — 事件详情页 ✅

| 编号 | 任务 | 状态 |
|------|------|------|
| T3.1 | 详情页开发 — `/events/{id}`，展示标题/来源/摘要/ESG 议题（SASB）；404 友好提示 | ✅ |
| T3.2 | 首页→详情链路 — ContentCard 主点击→详情页，详情页返回首页 | ✅ |

**验收**：`/events/{id}` 字段完整 ✅ | 10 条 SSG 静态生成 ✅ | 旧字段（判定依据/影响/动作）无残留 ✅ | 自定义 404 ✅ | 首页→详情链路通 ✅ | `npm run build` ✅

### M4 — 视觉打磨与响应式 ✅

| 编号 | 任务 | 状态 |
|------|------|------|
| T4.1 | 响应式栅格 — 手机 1 栏 / 平板 2 栏 / 桌面 3 栏 | ✅ |
| T4.2 | Tab 移动端滑动 — `overflow-x-auto` + `shrink-0`，手指可滑 | ✅ |
| T4.3 | 空态处理 — Tab 无内容友好提示 + 自定义 404 + SSG 无首屏空白 | ✅ |
| T4.4 | 顶栏 Logo 占位 — 虚线边框容器 + "Logo 待 AI design" | ✅ |
| T4.5 | 字号与间距审计 — 正文 ≥14px，行距宽松，卡片呼吸感 | ✅ |

### M5 — 验收与发布 ✅

| 编号 | 任务 | 状态 |
|------|------|------|
| T5.1 | 自查验收 — `npm run build` / 双页可访问 / 首页→详情流程 / 栅格断点 / 空态 | ✅ |
| T5.2 | 部署 — 支持 `next build` + `next start`，可一键部署 Vercel | ✅ |
| T5.3 | 最终验收 — 明卷 + 暗卷验收 | ✅ |

---

## 最终验收清单

### Build 检查
```
▲ Next.js 16.2.12 (Turbopack)
✓ Compiled successfully
✓ TypeScript check passed
✓ 15/15 static pages generated

Route (app)
┌ ○ /                     ← 首页
├ ○ /_not-found           ← 404 页面
├ ƒ /api/review            ← 审校 API
├ ● /events/[id]           ← 10 条事件详情（SSG）
│  ├ /events/m-eu-csddd
│  ├ /events/m-eu-cbam
│  ├ /events/m-exp-omnibus
│  └ [+7 more paths]
└ ○ /review                ← AI 草稿审校页
```

### 页面可访问性
| 页面 | 路径 | 访问方式 | 结果 |
|------|------|----------|------|
| 首页 | `/` | SSG | ✅ |
| 事件详情 | `/events/{id}` | SSG（10 条） | ✅ |
| 审校页 | `/review` | 客户端渲染 | ✅ |
| 404 | `/events/nonexist` | 自定义 404 | ✅ |

### 核心流程
```
首页 → 浏览卡片 → 点击卡片 → 事件详情页
  ├ 标题 / 来源 / 摘要 / ESG 议题（SASB）
  └ 右上角「阅读原文」跳信源原链接

首页 Tab 切换：全部 / 政策 / 专家观点 / 学术 / 评级
  └ 过滤 contentType，空态友好提示
```

### Mock 数据配比
| 类型 | 数量 | 示例 |
|------|------|------|
| ESG 政策 | 3 | CSDDD、CBAM、新加坡 ISSB |
| 专家观点 | 3 | 综合简化法案、CBAM 中小企业冲击、ISSB 亚洲落地 |
| 学术文章 | 2 | ESG 评级分歧、尽职调查溢出效应 |
| 评级动态 | 2 | MSCI 模型调整、EcoVadis 方法更新 |

---

## 项目文件结构

```
esg-radar-mvp/
├─ app/
│  ├─ page.tsx              ← 首页（SSG）
│  ├─ not-found.tsx         ← 自定义 404
│  ├─ layout.tsx            ← 全局布局（顶栏 + Logo 占位）
│  ├─ globals.css           ← Tailwind 主题色 + 全局样式
│  ├─ events/[id]/
│  │  └─ page.tsx           ← 事件详情页（SSG + generateStaticParams）
│  ├─ review/
│  │  └─ page.tsx           ← AI 草稿审校页
│  └─ api/review/
│     └─ route.ts           ← 审校 API（GET + PUT）
├─ components/
│  ├─ HomeClient.tsx        ← 首页客户端交互（Tab + 栅格）
│  └─ ContentCard.tsx       ← 事件卡片组件
├─ data/
│  ├─ sources.json          ← 信源配置（8 个初始信源）
│  ├─ seen_hashes.json      ← 去重记录
│  └─ contents.json         ← 管道输出（运行时由 pipeline 产生）
├─ lib/
│  └─ esg-data.ts           ← 数据层（JSON 文件优先，降级 mock）
├─ scripts/
│  ├─ pipeline.mjs          ← 主管线（RSS 抓取 → LLM 结构化 → 发布）
│  └─ llm-structurer.mjs    ← LLM 结构化脚本
└─ .github/workflows/
   └─ pipeline.yml          ← 定时触发（周一/五 08:00）
```

---

## 部署说明

### 本地运行
```bash
npm run dev      # 开发服务器（HMR）
npm run build    # 构建生产版本
npm start        # 启动生产服务器
```

### 生产部署
推荐部署到 **Vercel**（Next.js 原生平台）：
1. 将仓库推送到 GitHub
2. 在 Vercel 中导入项目
3. 框架自动识别 Next.js，零配置部署

当前 `next.config.ts` 未启用 `output: 'export'`，支持 SSR API 路由（`/api/review`）。

---

## 后续方向

- Logo 设计：顶栏预留了虚线占位容器（标注 "Logo 待 AI design"）
- 信源扩展：`data/sources.json` 可添加更多信源
- 实时数据：当前基于 mock + pipeline 管道，可过渡到真实 API
- 权重排序：首页 Tab 筛选按时间排序，后续可加入 prioritySort 权重逻辑
- 企业影响分析：事件详情页预留了字段结构，待评估不同利益相关方影响
