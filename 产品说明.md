# ESG Compass — 产品说明文档（v3）

> v3 更新于 2026-07-28，取代 v1（原文见 `产品说明.v1.md`）。
> 重写原因：v1 按"成熟情报运营产品"设计，9 个页面超出 MVP 验证需要；v2 收敛到"3 分钟扫读"单一核心场景。

---

## 一、产品定位与目标

**一句话目标**：让网页浏览者 3 分钟扫完当下全球 ESG 变化趋势，并能够对重点领域相关的文件找到原文和解读。

**目标用户**：ESG咨询顾问/企业ESG管理人员。核心场景只有一个：打开 → 扫重点 → 了解当前关注的 ESG 议题动态。

## 二、产品名称

- **产品名**：ESG Compass
- **项目代号**：esg-compass

## 三、页面结构（MVP 只留 2 页）

### 3.1 首页 `/`

单列布局，从上到下两个区块：

| 区块 | 内容 |
|---|---|
| 今日重点 | 页面头部：标题 + 一句话简介 |
| 时间轴 | 垂直时间轴布局，按日期分组。左侧绿色圆点标记日期，右侧为事件卡片。分类Tab（全部/政策/专家观点/学术/评级）可切换过滤。每张卡片显示：内容类型、地区、日期、标题、摘要、ESG议题（SASB）、所属专区。卡片可点击进入事件详情页；右上角原文图标跳信源原链接。 |

首页内容按发布时间由近到远排列。不放指标卡、不放独立评级与奖项区块。
展示：标题、来源、摘要、所属 ESG 议题（参照 SASB 分类体系）。

### 3.3 知识专区首页 `/zones`

按核心议题分类的知识专区概览页面，以卡片网格展示每个专区的名称、简介、关联事件数及关键里程碑数。

| 区块 | 内容 |
|---|---|
| 专区卡片网格 | 每个专区显示名称、描述、关联事件数、里程碑数。点击进入专区详情 |

初期包含三个专区：ESG 信息披露、ESG 评级、可持续供应链。后续可扩展。

### 3.4 知识专区详情 `/zones/{id}`

展示指定专区的完整信息：

| 区块 | 内容 |
|---|---|
| 专区头部 | 名称 + 描述 |
| 关键里程碑 | 时间线形式展示该议程领域的关键政策节点（按时间倒序排列），每个里程碑包含日期、标题、摘要 |
| 关联事件 | 与该专区相关的 ESG 事件卡片列表，点击可跳转事件详情页 |

专区页面的核心价值：让浏览者在关注"今天发生了什么"的同时，能回溯"这个议题的完整脉络"。时间线覆盖过去 2-5 年的关键节点，解决新政策/评级/法规的历史上下文缺失问题。

## 四、不做清单（v2 从 v1 砍掉）

以下模块说明删除、不建代码：AI 检索、简报生成、工作台、搜索与发现、订阅中心、运营后台、评级与奖项专栏、分析中心。

砍掉的理由：它们都是"运营一个成熟情报产品"才需要的机器，而 MVP 要验证的只有一件事——把外部 ESG 变化翻译成"跟我有没有关系"的判断是否值钱。验证通过前不建分发与运营管道。

## 五、数据模型

### 内容类型（ContentType）
```
ESG 政策 | 专家观点 | 学术文章 | 评级动态
```
- **ESG 政策**：含绿色贸易、双碳、ESG 披露、可持续发展等政策法规
- **专家观点**：对 ESG 政策的点评与解读
- **学术文章**：相关研究与学术分析
- **评级动态**：主流 ESG 评级的口径与结果变化

### 专区类型（Zone）
```
id          - 唯一标识
name        - 专区名称
description - 专区描述
eventIds    - 关联事件 ID 列表
milestones  - 关键里程碑（日期 + 标题 + 摘要）
```

### 内容字段（ContentItem）
```
id              - 唯一标识
title           - 标题
contentType     - 内容类型
region          - 地区
publishedAt     - 发布日期
importanceLevel - 高/中/低
summary         - 摘要
sourceName      - 信源名称
sourceUrl       - 信源原链接（所有卡片跳转此链接）
esgTopic        - 所属 ESG 议题（参照 SASB 分类体系）
```

相对 v1 的字段调整：删除 `businessImpact`、`whyImportant`、`actions`（对利益相关方的影响较难标准化判断，优先保证信息聚合的准确性）、删除 `riskTags`、`topicTags`、`suggestedOwners`、`reviewStatus`/`reviewAction`/`reviewNote`（运营后台已砍或复用度低）。新增 `esgTopic`（SASB 议题分类，确保每条内容可归入明确的 ESG 议题）。

### Mock 数据（约 10 条）

配比为弹性要求，不锁定具体数字：

- 四个类别每类至少 1 条，保证首页分类 Tab 无空档
- ESG 政策与专家观点为主力，两类合计占比过半
- 学术文章与评级动态适量点缀即可


## 六、数据管道

### 6.1 设计原则（派生自 AI Hot 效率模型）

分层入池，不做无差别批量消耗。

| 原则 | 对 ESG 快讯的意义 |
|---|---|
| 固定信源池，不搜全网 | 全球 ESG 信息来源已知且有限（EU / MAS / MSCI / CDP / SSRN / 若干智库），不需要搜索引擎 |
| 服务端筛好再给模型 | 信源按 contentType 预分类，LLM 不猜"这是政策还是学术"；无 RSS 的页面只抓列表页，不爬全文 |
| 只取增量 | RSS 条件 GET（304 → 零成本）；新增条目才调用 LLM |
| 够用就好 | 每次触发约 3–8 条新增，~3–15 美分 LLM 费用 |
| 不编造 | LLM 只结构化已有内容，不补背景知识；判定依据可标注"AI 初稿" |

### 6.2 完整数据流

`
信源清单（sources.json）
  │
  ├─ RSS 信源 ──────── 带 If-Modified-Since / ETag 定时轮询
  │                     304 → 跳过
  │                     200 → 解析 feed → 与已处理 hash 对比
  ├─ 无 RSS 信源 ──── 按触发执行 Playwright / cheerio 抓列表页
  │                     提取标题 + 链接 → 与已处理 hash 对比
  │
  ▼ 仅对新增条目
按 contentType 选择对应的 LLM Prompt → 生成 ContentItem 初稿（JSON），标注 aiDraft: true
  │
  ▼ 直接写入
contents.json（已发布数据，含 aiDraft 标记）
  │
  ▼ 你随时可以审查（非阻塞）
发现错误 → 在后台修改 → 内容更新
  │
  ▼ Next.js build 编译
首页 + 详情页
`

### 6.3 信源配置（sources.json）

每条信源只改这一个配置文件，即可加入或移除，不需要改动管道代码：

`
[
  {
    "id": "eu-commission",
    "name": "欧盟委员会司法与消费者总司",
    "url": "https://commission.europa.eu/",
    "type": "rss",
    "contentType": "ESG 政策",
    "region": "欧盟",
    "language": "en",
    "enabled": true
  },
  ...
]
`

初始信源清单（待你确认和补充后锁定）：

| 信源 | 类型 | contentType | URL |
|---|---|---|---|
| 欧盟动态（EUR-Lex） | RSS（地址待确认） | ESG 政策 | — |
| SEC 新闻稿 | RSS | ESG 政策 | https://www.sec.gov/news/pressreleases.rss |
| IFRS / ISSB 新闻 | RSS（地址待确认） | ESG 政策 | — |
| Eco-Business | RSS | 专家观点 | https://www.eco-business.com/feed/ |
| WRI Insights | RSS（地址待确认） | 专家观点 | — |
| CDP 文章 | 可爬取 | 评级动态 | https://www.cdp.net/en/articles |
| SSRN ESG 论文 | 可爬取 | 学术文章 | https://papers.ssrn.com/sol3/JELJOUR_Results.cfm?jrnl_id=2774117 |
| MAS 可持续金融 | 可爬取 | ESG 政策 | https://www.mas.gov.sg/development/sustainable-finance |

你补完信源清单后，Pipeline 代码直接读取这份配置。

### 6.4 LLM Prompt 模板

每种 contentType 独立模板，减少无用的通用分类消耗。以政策类为例：

`
你是一份 ESG 情报的结构化编辑助手。收到一条原始资讯后，严格按以下 JSON 格式输出。

原始内容：
{title}
{summary / body}

输出：
{
  "summary": "140 字以内的中文摘要，
  "esgTopic": "所属 ESG 议题，从 SASB 分类体系中选择最匹配的一项（如：温室气体排放 / 气候风险 / 供应链管理 / 水资源管理 / 人权与劳工 / 合规与监管 / 数据安全 / 社区关系 / 废弃物管理 / 产品质量与安全）",
  "importanceLevel": "高 | 中 | 低"
}
`

### 6.5 触发方式与成本预估
触发方式：自动触发——每周一、周五 08:00 GitHub Actions 执行一次全流程（fetch → LLM → 已发布，aiDraft 标记）。手动触发——运行 `npm run pipeline` 或后续后台设同步按钮。


| 项目 | 量级 |
|---|---|
| 信源轮询 | 每次最多约 10 次 HTTP 请求，95% 返回 304（无成本） |
| LLM 结构化调用 | 每次抓取约 3–8 次，每次约 1–2 美分 |
| 单次执行成本 | 约 0.5–1 元人民币 |
| 你的人工投入 | 每周两轮，每轮约 5–15 分钟校对 |

### 6.6 信源扩展方式

在 sources.json 追加一条即可，无需改管道代码。例如新增一个 ESG 政策类 RSS：

`
{ "id": "cfpb-esg", "name": "CFPB ESG 相关", "url": "...", "type": "rss", "contentType": "ESG 政策", "region": "美国", "language": "en", "enabled": true }
`

下次触发时自动纳入轮询，零代码改动。


## 七、技术栈

- **框架**：Next.js 16 (App Router)
- **样式**：Tailwind CSS 4 + 自定义 CSS 变量
- **字体**：系统字体栈（中文正文落 PingFang SC / Microsoft YaHei）
- **数据库**：本阶段 mock 数据，后续迁移 Prisma + libSQL

## 八、项目结构

```
esg-compass/
```
esg-compass/
├── app/
│   ├── layout.tsx          # 全局布局（简洁顶栏，无侧边导航）
│   ├── globals.css         # 全局样式
│   ├── page.tsx            # 首页
│   └── events/[id]/        # 事件详情
├── components/
│   └── ...                 # 卡片等展示组件
├── app/zones/           # 专区页面
│   ├── page.tsx          # 专区列表
│   └── [id]/page.tsx     # 专区详情
├── components/
│   └── ...               # 卡片等展示组件
```
└── lib/
    ├── esg-data.ts         # 内容数据与类型定义
    └── priority-rules.ts   # 重要性判定与排序规则
```

v1 骨架中已建但被砍模块对应的文件（简报生成、信源同步、后台存储、检索、聚合分析），开发轮一并删除。

## 九、验收线（开发轮）

1. 开发轮第一件事：`git init` 建基线（当前目录无版本控制，出错无回滚点）。
2. `npm run build` 通过。
3. `/` 与 `/events/{id}` 两个页面可访问。
4. "浏览首页 → 点击卡片 → 阅读详情"流程用 mock 数据跑通。无检索环节。

## 十、启动方式

```bash
cd esg-radar-mvp
npm run dev
# 访问 http://localhost:3000
```

## 十一、已完成功能

以下功能已实现并上线：

1. **精选推荐理由（LLM-as-Judge + 三维度）** — 2026-07-28
   - 紧迫性 / 广度 / 可转化性三维度打分，10 条 mock 中 4 条精选
   - 卡片展示推荐理由，详情页同步显示
   - 首页精选切换按钮，独立于分类 Tab 筛选

2. **运营看板 /ops** — 2026-07-29
   - 信源状态监控（健康度/最后一次抓取/成功失败次数）
   - 管道概览（今日抓取/LLM处理/精选/发布）
   - 异常日志（信源失败/LLM错误/网络异常）
   - 密码保护（中间件拦截，/login 登录）
   - 内置功能路线图面板（实时反映项目进度）

3. **时间轴美化** — 2026-07-29
   - 垂直时间轴 + 按日期分组 + 圆点连接线
   - 同日期事件聚合为卡片组，细线分割
   - 20 条截断 + "展示更多"按钮
   - 首页去"今日重点"header

## 十二、待完善功能

以下功能已明确方向，暂未实现。所待办项同步维护在 lib/roadmap.ts（在 /ops 后台"功能路线图"面板可见），Codex 每次启动时会主动扫描提醒。

1. **ESG 专区建设（新增/编辑/删除专区）**
   - 当前专区为硬编码 mock 数据，后续应支持：
     - 新增专区（从后台配置新专题）
     - 设置专区覆盖的事件范围（手动选择事件 / 按 contentType+议题 自动聚合）
     - 专区图片/Icon 上传

2. **增强检索（AI 检索）**
   - 基于知识库的语义检索，支持自然语言提问
   - 检索范围覆盖历史事件 + 专区里程碑
   - 每条检索结果附带来源引用

3. **增强检索依赖的知识库建设**
   - 建立 ESG 政策全文库：从信源下载 PDF/HTML 原文并结构化存储
   - 建立框架标准库：SASB / GRI / ISSB / TCFD 等标准文本入库
   - 事件 → 知识单元沉淀：每次 pipeline 产出的事件自动归入知识库
   - 向量化索引：对标题、摘要、正文生成语义向量，支持相似度匹配
